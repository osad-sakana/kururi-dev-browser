# KururiDevBrowser v0.1 開発プラン

## コンセプト

Web開発者向けのレスポンシブプレビュー特化ブラウザ。URLを入力すると複数デバイスサイズで同時表示し、スクロール同期・スクリーンショット保存が可能。

## 技術スタック

| レイヤー | 技術 | 理由 |
|---------|------|------|
| フレームワーク | Electron | `webview`タグでX-Frame-Options制約を回避 |
| UI | React + TypeScript | Electron移行前提でも変更不要 |
| ビルド | Vite + electron-builder | 高速HMR + クロスプラットフォームビルド |
| スタイル | Tailwind CSS | ユーティリティベースで素早くUI構築 |
| スクリーンショット | `webview.capturePage()` | Electron標準APIで各ビューポート個別キャプチャ |

## アーキテクチャ

```
┌─────────────────────────────────────────────────┐
│ Main Process (Electron)                         │
│  - ウィンドウ管理                                │
│  - スクリーンショット保存（dialog + fs）          │
│  - デバイスプリセット管理                         │
└────────────────────┬────────────────────────────┘
                     │ IPC
┌────────────────────▼────────────────────────────┐
│ Renderer Process (React)                        │
│ ┌─────────────────────────────────────────────┐ │
│ │ URLバー + コントロール                       │ │
│ ├─────────────────────────────────────────────┤ │
│ │ ViewportContainer                           │ │
│ │ ┌───────┐ ┌──────────┐ ┌────────────────┐  │ │
│ │ │Mobile │ │ Tablet   │ │ Desktop        │  │ │
│ │ │375x667│ │ 768x1024 │ │ 1440x900       │  │ │
│ │ │       │ │          │ │                │  │ │
│ │ │webview│ │ webview  │ │ webview        │  │ │
│ │ └───────┘ └──────────┘ └────────────────┘  │ │
│ └─────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

## 機能仕様

### 1. URL入力 & ナビゲーション

- 上部にURLバーを配置
- Enter or ボタンで全webviewに同時ロード
- ローディング状態表示（各ビューポートごと）
- 戻る/進む/リロードボタン（全webview連動）

### 2. マルチビューポート同時表示

- 横並びで複数デバイスサイズを同時レンダリング
- 各ビューポートはElectronの`<webview>`タグを使用
- ビューポート上部にデバイス名とサイズを表示
- ドラッグでビューポートの幅を手動リサイズ可能

**プリセットデバイス（v0.1）:**

| デバイス名 | 幅 | 高さ |
|-----------|-----|------|
| iPhone SE | 375 | 667 |
| iPhone 14 Pro | 393 | 852 |
| iPad | 768 | 1024 |
| iPad Pro 12.9 | 1024 | 1366 |
| Laptop | 1440 | 900 |
| Desktop | 1920 | 1080 |

- 表示するデバイスをチェックボックスで選択
- カスタムサイズの追加（幅 x 高さを手入力）

### 3. スクロール同期

- トグルでON/OFF切り替え
- あるビューポートをスクロール → 他の全ビューポートが同じ比率でスクロール
- 実装: `webview.executeJavaScript()`でscrollイベントをリッスンし、IPCでRenderer → 他のwebviewへブロードキャスト
- スクロール位置はピクセルではなく**割合（%）ベース**で同期（ビューポートごとにページ高さが異なるため）

### 4. スクリーンショット保存

- 「全ビューポートをキャプチャ」ボタン
- 各webviewの`capturePage()` APIで個別にキャプチャ
- 保存形式: PNG
- ファイル名: `{URL_host}_{デバイス名}_{幅}x{高さ}_{日時}.png`
- 保存先: ダイアログで選択 or デフォルトフォルダ
- 一括保存: 選択したフォルダに全デバイス分を一度に保存

## ファイル構成

```
kururi-dev-browser/
├── package.json
├── electron-builder.yml
├── vite.config.ts
├── tsconfig.json
├── src/
│   ├── main/                    # Electron Main Process
│   │   ├── index.ts             # アプリ起動、ウィンドウ生成
│   │   ├── ipc-handlers.ts      # IPC通信ハンドラ
│   │   └── screenshot.ts        # スクリーンショット保存ロジック
│   ├── renderer/                # React UI
│   │   ├── App.tsx
│   │   ├── components/
│   │   │   ├── URLBar.tsx       # URL入力 + ナビゲーション
│   │   │   ├── ViewportContainer.tsx  # ビューポート一覧
│   │   │   ├── ViewportPanel.tsx      # 個別ビューポート
│   │   │   ├── DeviceSelector.tsx     # デバイス選択UI
│   │   │   └── Toolbar.tsx            # スクロール同期/SS等のコントロール
│   │   ├── hooks/
│   │   │   ├── useScrollSync.ts       # スクロール同期ロジック
│   │   │   └── useViewports.ts        # ビューポート状態管理
│   │   ├── types/
│   │   │   └── device.ts              # デバイスプリセット型定義
│   │   └── presets.ts                 # デバイスプリセットデータ
│   └── preload/
│       └── index.ts             # webview preload（スクロール検知）
└── resources/
    └── icon.png
```

## 実装順序

### Phase 1: 基盤（〜2日）

1. Electron + React + Vite + TypeScriptのボイラープレート構築
2. メインウィンドウ生成、基本レイアウト
3. URLバーの実装

### Phase 2: マルチビューポート（〜3日）

4. webviewタグによる単一ビューポート表示
5. 複数ビューポート同時表示
6. デバイスプリセット選択UI
7. カスタムサイズ追加

### Phase 3: スクロール同期（〜2日）

8. preloadスクリプトでscrollイベント検知
9. IPC経由で他ビューポートへスクロール位置ブロードキャスト
10. 割合ベース同期のチューニング（無限ループ防止含む）

### Phase 4: スクリーンショット（〜1日）

11. `capturePage()` APIによるキャプチャ実装
12. ファイル名生成 + 保存ダイアログ
13. 一括保存機能

### Phase 5: 仕上げ（〜1日）

14. UIの調整・レイアウト最適化
15. エラーハンドリング（URL不正、読み込み失敗等）
16. macOS向けビルド確認

## 技術的な注意点

- **webviewのセキュリティ**: `webpreferences="contextIsolation=true"` を設定。preloadスクリプトは最小限に
- **スクロール同期の無限ループ防止**: スクロールイベントの発火元を識別するフラグを持ち、プログラム的スクロールでは再発火しないようにする
- **パフォーマンス**: webviewを多数開くとメモリ消費が大きい。v0.1では最大6ビューポートを上限とする
- **User-Agent**: 各webviewにデバイスに応じたUA文字列を設定し、モバイル版ページを正しく取得する

## 将来のロードマップ（v0.2以降）

- HTMLファイルのドラッグ&ドロップ入力
- テキストエリアにHTML貼り付けてプレビュー
- ローカルファイル監視 + 自動リロード（ホットリロード）
- DevToolsパネル統合
- ネットワークスロットリング（3G/4Gシミュレーション）
- ダークモード
- タッチイベントシミュレーション
- ビューポートの縦横切り替え（ポートレート/ランドスケープ）
